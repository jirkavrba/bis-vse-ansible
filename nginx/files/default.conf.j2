server {
    listen [::]:80;
    listen 80;

    server_name {{ server_host }};
    server_tokens off;

    location /.well-known/acme-challenge {
      allow all;
      root /var/www/certbot;
    }
}